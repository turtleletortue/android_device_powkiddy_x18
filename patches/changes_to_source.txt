system/core/init/init.cpp

Changed -

        if (!WriteFile("/sys/fs/selinux/checkreqprot", "0", &err)) {
            LOG(ERROR) << err;
            security_failure();
        }

Became -

        if (!WriteFile("/sys/fs/selinux/checkreqprot", "0", &err)) {
            //LOG(ERROR) << err;
            //security_failure();
        }
